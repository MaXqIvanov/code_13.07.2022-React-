import React from 'react'
import styles from '../../scss/OrdersPage.module.scss';

export const ModalAddressAdded = () => {
  return (
    <div className={styles.address_added}>
        <div className={styles.address_added_wrapper}>
            добавление адресса
        </div>
    </div>
  )
}
